[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[project]
name = "datapod"
version = "0.3.0"
description = "Python bindings for the datapod library"
requires-python = ">=3.9"

[tool.maturin]
features = ["python"]
module-name = "datapod"
