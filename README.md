# datapod_rs

Rust port of `../datapod`, starting from the foundational type and container layers that the rest of the robolibs stack depends on.

The immediate goal is to establish a clean crate shape and a staged porting plan before implementing the core modules.
